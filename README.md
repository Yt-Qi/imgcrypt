# Local Image Encrypt / Decrypt Tool

Single-page HTML tool by QiLi for local image encryption and decryption.

## GitHub Pages

This repository is intended to be deployed as a GitHub Pages project site.
The public URL should look like:

```text
https://yt-qi.github.io/imgcrypt/
```

The app entry point is `index.html`.
